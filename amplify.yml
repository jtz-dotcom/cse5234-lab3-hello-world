version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No package install needed"
    build:
      commands:
        - mkdir -p dist && cp index.html dist/index.html
  artifacts:
    baseDirectory: dist
    files:
      - "**/*"
